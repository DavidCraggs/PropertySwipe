
import { createViewingRequest } from '../src/lib/storage';


// Mock the storage module if needed, or just run it and see if it fails
// For now, let's just try to import it and see if it compiles/runs

console.log('Successfully imported createViewingRequest', createViewingRequest);
